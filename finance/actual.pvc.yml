apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: actual-pvc-config
  namespace: finance
spec:
  storageClassName: manual
  volumeName: actual-pv-config
  accessModes:
    - ReadWriteMany
  volumeMode: Filesystem
  resources:
    requests:
      storage: 5Gi
