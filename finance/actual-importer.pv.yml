apiVersion: v1
kind: PersistentVolume
metadata:
  name: actual-importer-pv-config
  namespace: finance
spec:
  storageClassName: manual
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  hostPath:
    path: "/mnt/t5/config/actual-importer"
