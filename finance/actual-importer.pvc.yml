apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: actual-importer-pvc-config
  namespace: finance
spec:
  storageClassName: manual
  volumeName: actual-importer-pv-config
  accessModes:
    - ReadWriteMany
  volumeMode: Filesystem
  resources:
    requests:
      storage: 5Gi
